library(methods)

dataset <- setRefClass("Dataset", fields = list(
                                    ds = "Dataset"),
                                    methods = list(
                                    ))